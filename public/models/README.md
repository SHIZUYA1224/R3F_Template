# GLB Drop Folder

`model.glb` is the default file loaded by the template.

Export from Blender as `public/models/model.glb`, then run:

```bash
npm run dev
```

Additional files can be loaded from the control panel with paths like `/models/character.glb`.
